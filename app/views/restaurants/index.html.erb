<h1>All the Restaurants</h1>
<%= link_to 'Create Restaurant', new_restaurant_path %>
<% @restaurants.each do |restaurant| %>
  <h2><%= link_to restaurant.name, restaurant_path(restaurant) %> <%= '⭐️' * restaurant.rating %></h2>
  <p><%= restaurant.address %></p>
  <% end %>
